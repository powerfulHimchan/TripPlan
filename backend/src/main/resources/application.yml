spring:
  application:
    name: tripplan-backend
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/tripplan}
    username: ${DB_USERNAME:tripplan}
    password: ${DB_PASSWORD:tripplan}
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        jdbc.time_zone: UTC
  flyway:
    enabled: true

tripplan:
  auth:
    jwt-secret: ${JWT_SECRET:tripplan-local-development-secret-key-change-me}
    token-valid-days: ${JWT_VALID_DAYS:30}
  notification:
    fcm-enabled: ${FCM_ENABLED:false}

server:
  error:
    include-message: always
